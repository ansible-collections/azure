- set_fact:
    rpfx: "{{ resource_group | hash('md5') | truncate(7, True, '') }}{{ 1000 | random }}"

- name: get all Microsoft application
  azure_rm_msapplication_info:
    all: True
  register: output

- assert:
    that:
      - output.applications | length == 1
