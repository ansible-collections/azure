- name: set guid for registration assignment id
  set_fact:
    subscription_id: "{{ azure_subscription_id }}"
    managed_by_tenant_id: "{{ azure_managed_by_tenant_id }}"
    principal_id: "{{ azure_principal_id }}"
    role_definition_id: "{{ azure_role_definition_id }}"
  run_once: yes

- name: Create a RegistrationDefinition
  azure_rm_registrationdefinition:
    scope: subscriptions/{{ subscription_id }}
    properties:
      description: first_test
      authorizations:
        - principal_id: "{{ principal_id }}"
          role_definition_id: "{{ role_definition_id }}"
      managed_by_tenant_id: "{{ managed_by_tenant_id }}"
      registration_definition_name: test_def
  register: def_output

- name: Create a RegistrationAssignment ---check mode
  azure_rm_registrationassignment:
    scope: subscriptions/{{ subscription_id }}
    properties:
      registration_definition_id: "{{ def_output.state.id }}"
  register: output
  check_mode: yes

- assert:
    that:
      - output.changed 

- name: Create a RegistrationAssignment 
  azure_rm_registrationassignment:
    scope: subscriptions/{{ subscription_id }}
    properties:
      registration_definition_id: "{{ def_output.state.id }}"
    expand_registration_definition: true
  register: ass_output

- assert:
    that:
      - ass_output.changed

- name: Create a RegistrationAssignment -- idempotent
  azure_rm_registrationassignment:
    scope: subscriptions/{{ subscription_id }}
    registration_assignment_id:  "{{ ass_output.state.name }}"
    properties:
      registration_definition_id: "{{ def_output.state.id }}"
    expand_registration_definition: true
  register: output

- assert:
    that:
      - not output.changed

- name: Get a RegistrationAssignment
  azure_rm_registrationassignment_info:
    scope: subscriptions/{{ subscription_id }}
    registration_assignment_id:  "{{ ass_output.state.name }}"
    expand_registration_definition: true
  register: output

- assert:
    that:
      - output.registration_assignments[0].properties.registration_definition_id == "{{ def_output.state.id }}"

- name: Delete the RegistrationAssignment
  azure_rm_registrationassignment:
    scope: subscriptions/{{ subscription_id }}
    registration_assignment_id:  "{{ ass_output.state.name }}"
    state: absent

#- name: Delete the registration definition
#  azure_rm_registrationdefinition:
#    registration_definition_id: "{{ def_output.state.name }}"
#    scope: subscriptions//{{ subscription_id }}
#    state: absent
